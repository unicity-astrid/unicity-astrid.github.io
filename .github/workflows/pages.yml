name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

env:
  AOS_INSTALLER_SOURCE_COMMIT: dfa1d71c2737a016d8d4dd169d0755ff624f6b50
  ORACLE_INSTALLER_SOURCE_COMMIT: 83d0b478a5c7d7331d75c983ab51e708c9c19065

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          path: unicity-aos-web
      - name: Check out pinned canonical installer source
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          repository: unicity-aos/aos-ce
          ref: ${{ env.AOS_INSTALLER_SOURCE_COMMIT }}
          path: aos-ce
          persist-credentials: false
      - name: Check out pinned Oracle installer source
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          repository: unicity-aos/oracles
          ref: ${{ env.ORACLE_INSTALLER_SOURCE_COMMIT }}
          path: oracles
          persist-credentials: false
      - name: Verify canonical installer mirror
        run: |
          test "$(git -C aos-ce rev-parse HEAD)" = "$AOS_INSTALLER_SOURCE_COMMIT"
          test "$(git -C oracles rev-parse HEAD)" = "$ORACLE_INSTALLER_SOURCE_COMMIT"
          sh -n unicity-aos-web/site/public/install.sh
          sh -n aos-ce/install.sh
          sh -n oracles/install.sh
          cmp aos-ce/install.sh unicity-aos-web/site/public/base-install.sh
          cmp oracles/install.sh unicity-aos-web/site/public/oracle-install.sh
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: unicity-aos-web/site/package-lock.json
      - name: Install
        working-directory: unicity-aos-web/site
        run: npm ci
      - name: Typecheck
        working-directory: unicity-aos-web/site
        env:
          ASTRO_TELEMETRY_DISABLED: '1'
        run: npm run check
      - name: Build
        working-directory: unicity-aos-web/site
        env:
          ASTRO_TELEMETRY_DISABLED: '1'
        run: npm run build
      - name: Verify built installer bytes
        run: |
          cmp unicity-aos-web/site/public/install.sh unicity-aos-web/site/dist/install.sh
          cmp aos-ce/install.sh unicity-aos-web/site/dist/base-install.sh
          cmp oracles/install.sh unicity-aos-web/site/dist/oracle-install.sh
      - name: Test release surfaces
        working-directory: unicity-aos-web/site
        run: npm test
      - uses: actions/upload-pages-artifact@56afc609e74202658d3ffba0e8f6dda462b719fa # v3
        with:
          path: unicity-aos-web/site/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@d6db90164ac5ed86f2b6aed7e0febac5b3c0c03e # v4
